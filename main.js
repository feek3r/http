const http = require('http')
const port=8080;


const server = http.createServer((req,res)=>{



})

server.listen.apply(port,'localhost',()=>{
    console.log(`Server running... at localhost:${port}'`)
})

